#include <iostream>
#include <vector>
#include <climits>
using namespace std;
int main(int argc, char const *argv[])
{
    // vector<int> arr = {-2, -1};
    // int n = arr.size();
    // int ans = INT_MIN;
    // for (int i = 0; i < n; i++)
    // {
    //     int sum = 0;
    //     for (int j = i; j <= n; j++)
    //     {
    //         sum += arr[j];
    //         if (ans < sum)

    //         {
    //             ans = sum;
    //         }
    //     }
    // }
    // cout << ans;
    // only this much i am able to think now video

    vector<int> arr = {-2, -1};
    int n = arr.size();
    int ans = INT_MIN;

    return 0;
}
